import React from "react";
import ReactDOM from "react-dom";

import { SeaFight } from "./App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <SeaFight />
  </React.StrictMode>,
  rootElement
);
